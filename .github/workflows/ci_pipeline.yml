name: Cyber-Trace CI Pipeline

on:
  push:
    branches: 
      - main
      - 'feature/**'
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Download source code
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Setup Java (needed for PySpark)
    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    # 3. Setup Python
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        cache: 'pip'

    # 4. Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        # Fallback: gdyby nie było pliku requirements.txt
        pip install pytest pyspark flake8 

    # 5. Linting
    - name: Lint with flake8
      run: |
        # E9, F63, F7, F82 -> Błędy składniowe (syntax errors)
        flake8 src tests --count --select=E9,F63,F7,F82 --show-source --statistics
        # Reszta reguł (ostrzeżenia)
        flake8 src tests --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 6. Run Unit Tests
    - name: Run Unit Tests
      env:
        PYTHONPATH: src
      run: |
        pytest tests/ -v